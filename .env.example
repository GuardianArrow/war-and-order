# Discord & OAuth (use Codespaces/Actions secrets in real envs)
DISCORD_TOKEN=
DISCORD_APP_ID=
DISCORD_PUBLIC_KEY=

# Database
MONGODB_URI=

# Guild-scoped config (non-secrets)
STAGING_GUILD_ID=
ANNOUNCEMENTS_CHANNEL_ID=

# Feature flags (boot defaults)
BOOT_FLAGS_COMMS_ENABLED=true
BOOT_FLAGS_MAINTENANCE=false
